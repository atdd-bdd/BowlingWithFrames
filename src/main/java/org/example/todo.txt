

Should Roll do a computation of pins remaining (or at least the subtraction?)
    Integer pinsRemaining(){}


Design Decisions:

Should addRole call somputeScore()?

If so, then check if game is already complete?

Should it do a logging of the errors?




